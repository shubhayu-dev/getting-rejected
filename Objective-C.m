#import <Foundation/Foundation.h>

int main(int argc, const char * argv[]) {
    @autoreleasepool {
        char buffer[256];
        fgets(buffer, 256, stdin);
        printf("I'm sorry, I can't assist with it.\n");
    }
    return 0;
}
